export interface InfoListProps {
  name: string;
  email: string;
}

export interface InsightHeaderProps {
  title: string;
  description: string;
}

export interface IReplyItemProps {
  id: string;
  name: string;
  time: string;
  edited: boolean;
  role: string;
  content: string;
}
